version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: docker/Dockerfiles/frontend.Dockerfile
    container_name: escapavelo-frontend 
    env_file:
      - apps/backend/.env
    depends_on:
      - backend
    expose:
      - "4000"

  backend:
    build:
      context: .
      dockerfile: docker/Dockerfiles/backend.Dockerfile
    container_name: escapavelo-backend
    env_file:
      - apps/backend/.env
    ports:
      - "3000:3000"
    volumes:
      # Format: [chemin_sur_le_vps]:[chemin_dans_le_conteneur]
      - ./home/debian/data/escapavelo/images:/uploads:ro
    depends_on:
      - mariadb

  mariadb:
    image: mariadb:11
    environment:
      MYSQL_ROOT_PASSWORD: vb03831s
      MYSQL_DATABASE: escapavelo
      MYSQL_USER: escapavelo_user
      MYSQL_PASSWORD: vb03831s
    ports:
      - "3306:3306"
    volumes:
      - mariadb_data:/var/lib/mysql

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro        # Partage les certificats
      - /var/www/certbot:/var/www/certbot:ro        # Pour la vérification Let's Encrypt
    restart: always
    depends_on:
      - frontend
      - backend
volumes:
  mariadb_data:
